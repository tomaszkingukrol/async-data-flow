#TODO TypeError: _run_sequence() takes 2 positional arguments but 3 were given
PIPEERRORPASSARGS = 'Passing args to {} is missing'
#TODO TypeError: Merge args error for bar - tu trzeba wymienic wszystkie funkcje concurent
PIPEERRORARGSMERGE = 'Merge args error for {}'

PIPEERRORROUTINE = 'Pipe must be build from routines: functions, methods or coroutines'
PIPEERRORARGS = 'Routines in pipe must use only positional or keyword parameters'
PIPEERRORCONARGS = 'Concurrent routines in pipe must have the same set of arguments'